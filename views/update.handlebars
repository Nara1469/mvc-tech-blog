<div class="row mt-4">
  <div class="col-md-12">
    <div class="card">
      <div class="card-header back-color">
        <label for="post-title">{{username}}'s Current Post</label>
      </div>
      <div class="card-body">
        <label for="post-title">Title:</label>
        <textarea id="post-title" name="post-title" rows="1">{{post.title}}</textarea>
        <label for="post-content">Content:</label>
        <textarea id="post-content" name="post-content" rows="3">{{post.content}}</textarea>
      </div>
      <button class="btn btn-primary" id="update-post" data-id="{{post.id}}">SUBMIT</button>
    </div>
  </div>
</div>